# FMEA-MWO 项目改造完整工作报告

> 日期: 2026-05-06 ~ 2026-05-07
> 分支: main
> 远程仓库: https://github.com/shushu0sama/FMEA-MWO.git

---

## 一、工作总览

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0. 模型迁移 | GPT-4o mini → DeepSeek V4 | ✅ |
| 1. 数据采集 | 下载 4 个外部 FMEA 数据集 | ✅ |
| 2. 数据处理 | 数据集格式统一 + 英文翻译 | ✅ |
| 3. 适配测试 | 测试现有模块对中文的兼容性 | ✅ |
| 4. PathExtraction 改造 | 重写 Cypher 查询适配中文 FMEA Schema | ✅ |
| 5. Generate 改造 | 中文 Prompt/Few-shot/黑名单 | ✅ |
| 6. Humanise 改造 | 中文缩写/拼音/字形/错字体系 | ✅ |
| 7. 端到端验证 | 全管线跑通 + 质量验证 | ✅ |

---

## 二、阶段 0：模型迁移（GPT-4o mini → DeepSeek V4）

### 变更文件（6 个代码 + 1 个文档）

| 文件 | 改动内容 |
|---|---|
| `Generate/llm_generate.py` | 添加 `DEEPSEEK_BASE_URL`/`DEEPSEEK_MODEL` 常量；3 处模型名 + 客户端 base_url |
| `Generate/llm_prompt.py` | 同上；1 处模型名 + 客户端 base_url |
| `Humanise/humanise.py` | 同上；1 处模型名 + 客户端 base_url |
| `Evaluation/evaluation.ipynb` | 客户端 base_url |
| `Generate/diversity_experiment.ipynb` | 6 处模型名 + 客户端 base_url |
| `Humanise/humanise_experiment.ipynb` | 1 处模型名 + 客户端 base_url |
| `README.md` | 更新说明模型切换 |

### 关键技术决策

- **API 端点**: `OpenAI(api_key=..., base_url="https://api.deepseek.com")`
- **最终模型**: `deepseek-chat`（映射至 DeepSeek V4 Flash）
- **原因**: `deepseek-v4-pro` 实测返回空响应（可能账户权限限制），V4 Flash 稳定可用且速度更快
- **兼容性**: DeepSeek API 完全兼容 OpenAI Python SDK，无需更换依赖

---

## 三、阶段 1-2：外部数据采集与处理

### 3.1 数据集下载

| 数据集 | 来源 | 状态 | 规模 | 许可 |
|---|---|---|---|---|
| **NHTSA 汽车投诉** | 美国国家公路交通安全管理局 | ✅ | 2,203,349 条 (1.56 GB) | 美国政府公开数据 |
| **StingRAY FMECA** | data.gov / 美国能源部 | ✅ | 17 份 xlsx | CC BY 4.0 |
| **Zenodo IMPACT FMECA** | EU Horizon 2020 | ✅ | 1 份 xlsx (6 sheets) | 开源 |
| **AMPEL360 航空安全** | GitHub | ⚠️ | 10 行 CSV | 开源 |
| **nbsdc.cn 温度变送器** | 国家基础学科数据中心 | ❌ | 需高校认证 | — |
| **HuggingFace PFMEA** | HuggingFace | ❌ | 网络不通 | — |

### 3.2 数据处理流程（三条路线）

#### Route A: 规则列映射（StingRAY + Zenodo）

```
17 份 FMECA xlsx → 关键词自动识别表头 → 列名映射 → 翻译 → 628 行
```

**StingRAY 表头解析**：每份文件 50 列多行合并表头，通过正则 `failure.*mode.*risk` 自动定位表头行 (row index 4)

**列映射关系**:
| 原始列 | 目标列 |
|---|---|
| Item Description (col 2) | 下一低分析层次 |
| Function (col 3) | 功能 |
| Failure Mode (col 6) | 故障模式 |
| Failure Effects (col 7) | 故障影响 |
| Failure Cause(s) (col 8) | 故障起因 |
| Risk Reduction Design (col 10) | 预防控制措施 |
| Post Failure Detection (col 12) | 探测控制措施 |
| Occurrence (col 14) | 发生度(O) |
| Human Safety (col 15) | 严重度(S) |

#### Route B: LLM 非结构化提取（NHTSA）

```
446 条投诉文本 → DeepSeek V4 Flash → JSON 提取 → 446 行 FMEA
```

**抽样策略**: 21 个汽车系统各取最多 20 条，分层抽样
**处理参数**: batch_size=10, temperature=0.3, 45 批次 100% JSON 解析成功率

#### Route C: 合并 + 英文清理

```
628 (StingRAY+Zenodo) + 446 (NHTSA) = 1,074 行
→ 三轮英文清理翻译 (803+183+12 个文本)
→ 字段派生补充 (下一低层次功能)
→ 最终 0 英文残留
```

### 3.3 翻译策略

| 轮次 | 翻译文本数 | 成功率 |
|---|---|---|
| 第一轮 | 1,002 个唯一英文文本 | 886/1,002 (88.4%) |
| 第二轮 | 183 个残留英文 | 171/183 (93.4%) |
| 第三轮 | 12 个最终残留 + 正则清理 | 100% |

**翻译参数**: model=deepseek-chat, temperature=0.2, batch_size=25-40

### 3.4 最终数据产出

**`fmea_merged_final.xlsx`**: 1,074 行 × 13 列 × 52 个系统

| 领域 | 系统数 | 行数 |
|---|---|---|
| 航空（原始） | 1 | 354 |
| 船舶能源 (StingRAY+Zenodo) | 22 | 628 |
| 汽车 (NHTSA) | 30 | 446 |

---

## 四、阶段 3：中文适配性测试（无损测试）

在**不修改任何代码**的前提下，测试三个现有模块对中文 FMEA 数据的兼容性：

### 4.1 PathExtraction 测试结果

```
现有 Cypher 查询使用标签: PhysicalObject, UndesirableEvent, Property...
中文 KG 实际标签: 故障模式, 故障起因, 关注要素层次...
→ 查询返回 0 条结果，完全不兼容
```

### 4.2 Generate 测试结果

| 输入 | 输出 | 评价 |
|---|---|---|
| `fuel pump` + `leaking` + 英文 prompt | `Fuel pump leaking, urgent repair needed.` | 正常 |
| `前舱主梁组件` + `主梁断裂` + 英文 prompt | `前舱主梁组件主梁断裂。` | 仅拼凑实体名 |
| 同上 + "用中文生成" 提示 | `前舱主梁组件发现断裂。` | 略有改善 |

结论: API 可输出中文但 prompt 模板、few-shot 示例全为英文设计，生成质量差。

### 4.3 Humanise 测试结果

| 子模块 | 英文机制 | 中文输入测试 |
|---|---|---|
| introduce_contractions | is not→isn't (112 条词典) | 0 变化 — 中文不存在形态变化 |
| introduce_abbreviations | service→svce (564 条词典) | 0 变化 — 词典全为英文 |
| rule_introduce_typos | QWERTY 键盘 + CMU 发音词典 | 0 变化 — 不适用于中文输入法 |

结论: 三个子模块对中文输入均无任何效果，完全不兼容。

---

## 五、阶段 4：PathExtraction 中文路径查询改造

### 5.1 新增查询

在 `path_queries.py` 中新增 `cn_direct_queries` (3 个) 和 `cn_complex_queries` (3 个):

| 查询 | 路径类型 | 提取数量 |
|---|---|---|
| cn_system_failure_cause | 系统→故障模式→故障起因 | 422 |
| cn_failure_effect | 故障模式→故障影响 | 135 |
| cn_system_component | 系统→组件层次 | 111 |
| cn_full_chain | 系统→故障模式→起因（全链路） | 422 |
| cn_prevention | 故障模式→预防控制措施 | 273 |
| cn_function_failure | 功能→故障模式 | 312 |
| **合计** | | **1,675** |

### 5.2 关键实现

```python
# 新增函数：直接从 Neo4j 提取中文路径
def get_cn_paths_from_neo4j(driver, valid=True):
    """ Extract paths directly from Chinese FMEA Neo4j KG """
    # 执行 6 个查询，统一映射为 {object_name, event_name, ...} 格式
    # 原有英文代码路径完全保留不变
```

### 5.3 输出格式统一

```
{
    "object_name": "前舱主梁组件",    # 设备/组件名
    "event_name": "主梁断裂",         # 故障事件名
    "cause": "主梁受到横向拉伸",      # 故障起因
    "system": "结构子系统",           # 所属系统
    ...
}
```

---

## 六、阶段 5：Generate 中文生成管线改造

### 6.1 中文 Prompt 模板（5×3 变体）

```python
CN_BASE_PROMPTS = [
    "根据以下设备故障信息，生成一条中文维护工单记录。",
    "你是维修技师，用简洁专业的中文写一条维护工单。",
    "为以下设备故障编写一条标准维护工单。",
    ...
]

CN_LIMIT_WORDS = [
    "语言简洁，每句不超过20个字。",
    "避免冗长表述，控制在15-20字以内。",
    ...
]

CN_LIMIT_COUNT = [
    "每条记录仅包含1-2句话。",
    "不添加多余解释，直接描述故障和处理。",
    ...
]
```

### 6.2 中文黑名单

```python
CN_BLACKLIST = ['发现', '检测到', '观察到', '显示有', '需要关注',
                '需要进行', '建议', '可能存在', '疑似', '似乎']
```

### 6.3 中文 Few-shot 示例

从 `data1014.xlsx` 提取 6 组设备故障对，用 LLM 生成真实工单风格示例：

| 设备 | 故障 | 示例工单 |
|---|---|---|
| 前舱主梁组件 | 主梁断裂 | 前舱主梁横向拉伸断裂需更换, 更换前舱主梁主梁已断, ... |
| 发动机冷却系统散热器 | 散热器堵塞 | 散热器水垢堵塞清理芯体, 拆检散热器疏通更换芯体, ... |
| 燃油泵 | 燃油泵泄漏 | 更换燃油泵密封圈处理渗油, 燃油泵底部渗漏拆检, ... |

### 6.4 新增函数

| 函数 | 功能 |
|---|---|
| `get_cn_generate_prompt()` | 随机组合中文 prompt 变体 |
| `get_cn_generate_fewshot()` | 加载中文 few-shot 示例 |
| `generate_cn_mwo()` | 单次中文 MWO 生成 |
| `generate_diverse_cn_mwo()` | 多样性中文 MWO 生成（多轮调用的去重） |
| `process_cn_mwo_response()` | 中文响应后处理（无 case folding） |
| `paraphrase_cn_mwo()` | 中文 MWO 改写 |

### 6.5 主入口支持双模式

```bash
# 中文管线
python llm_generate.py --cn --samples 10

# 英文管线（原有）
python llm_generate.py
```

---

## 七、阶段 6：Humanise 中文人性化体系重建

### 7.1 新建文件: `Humanise/humanise_cn.py`

### 7.2 词典构建

| 词典 | 条目数 | 示例 |
|---|---|---|
| **中文工业缩写** | 200+ | 发动机→引擎/发, 密封圈→O-RING, 液压作动筒→HYD CYL, 主起落架→MLG |
| **拼音混淆矩阵** | 120+ | 主→柱/住/注(zhǔ→zhù), 裂→烈/列/劣(liè), 变形→走形/弯了 |
| **字形混淆矩阵** | 30+ | 梁→粱, 泵→汞, 检→捡/睑, 封→卦 |
| **数字符号混用** | 6 | 一→1, 二→2, 第一→#1 |

### 7.3 实现函数

| 函数 | 功能 | 参数 |
|---|---|---|
| `cn_introduce_abbreviations()` | 术语替换（缩写/行业黑话） | chance=0.4 |
| `cn_introduce_typos()` | 中文错字模拟 | chance=0.08, max_typos=3 |
| `cn_replace_pinyin()` | 拼音混淆替换 | 40% 权重 |
| `cn_replace_shape()` | 字形混淆替换 | 15% 权重 |
| `cn_omit_char()` | 漏字模拟 | 20% 权重 |
| `cn_number_symbol_mixing()` | 中英数字混用 | 10% 概率 |
| `cn_humanise_sentence()` | 人性化主函数 | — |

### 7.4 人性化效果

```
原文: 机翼变形超限升力不足坠毁，修复结构子系统。
改后: 机翼弯了超限升力不足坠毁，修结构子系统。
      ↑ "变形"→"弯了"              ↑ "修复"→"修"

原文: 方向舵变形超限无法操控坠毁换舵查主起落架
改后: 方向舵歪了超限无法操控坠毁换舵查MLG
      ↑ "变形"→"歪了"               ↑ "主起落架"→"MLG"

原文: 燃油泵泄漏需要更换密封圈
改后: 油泵漏了需要换密封圈
      ↑↑↑ 三处变化
```

**实测变化率**: 57% (17/30 条被修改)

---

## 八、端到端验证

### 8.1 全管线测试

```
PathExtraction (1,675 条路径)
    → Generate (30 条中文 MWO)
        → Humanise (17/30 被修改, 57% 变化率)
            → 最终输出
```

### 8.2 输出文件

| 文件 | 说明 |
|---|---|
| `Generate/mwo_sentences/cn_log.txt` | 生成日志（设备+故障+5条工单） |
| `Generate/mwo_sentences/cn_log_humanised.txt` | 人性化对比（ORIG vs HUM） |
| `Generate/mwo_sentences/cn_synthetic.csv` | CSV 格式工单列表 |

### 8.3 质量指标

| 指标 | 数值 |
|---|---|
| 中文文本生成成功率 | 100% (30/30) |
| 人性化变化率 | 57% |
| 英文残留 | 0% |
| 错误或异常 | 0 |

---

## 九、文件变更汇总

### 本次改造涉及的文件

| Commit | 文件 | 变更类型 |
|---|---|---|
| 1st | `.gitignore` | 修改：排除 .env 和大文件 |
| 1st | `Evaluation/evaluation.ipynb` | 修改：DeepSeek API 迁移 |
| 1st | `Generate/llm_generate.py` | 修改：API 迁移 + 中文适配 |
| 1st | `Generate/llm_prompt.py` | 修改：API 迁移 + 中文 prompt |
| 1st | `Generate/diversity_experiment.ipynb` | 修改：API 迁移 |
| 1st | `Humanise/humanise.py` | 修改：API 迁移 |
| 1st | `Humanise/humanise_experiment.ipynb` | 修改：API 迁移 |
| 1st | `README.md` | 修改：模型说明更新 |
| 1st | `data/external/` (35 files) | 新增：外部数据集 + 处理脚本 + 产物 |
| 1st | `数据处理工作报告.md` | 新增 |
| 2nd | `PathExtraction/path_queries.py` | 修改：+6 个中文 Cypher 查询 |
| 2nd | `Generate/fewshot_messages/fewshot_cn.csv` | 新增：中文 few-shot 示例 |
| 2nd | `Humanise/humanise_cn.py` | 新增：中文人性化模块 |
| 2nd | `中文适配方案_RALPH.md` | 新增：适配方案文档 |

### 未修改的文件

| 文件 | 原因 |
|---|---|
| `data1014.xlsx` | 原始数据，不做任何修改 |
| `PathExtraction/maintie_to_kg.py` | MaintIE 英文导入逻辑保持不变 |
| `PathExtraction/path_matching.ipynb` | 原有的英文路径分析逻辑保持不变 |
| `Humanise/humanise.py` (主要逻辑) | 英文人性化逻辑保持不变 |
| `Generate/fewshot_messages/fewshot_generate.csv` | 英文 few-shot 保持不变 |
| `Evaluation/` 下的数据文件 | 评估数据保持不变 |

### 设计原则

- **双 Schema 共存**: 英文 MaintIE + 中文 FMEA 两套查询互不干扰
- **lang 参数切换**: 生成函数通过 `lang='cn'/'en'` 参数选择中英文逻辑
- **独立模块**: `humanise_cn.py` 作为新文件，不影响原有 `humanise.py`
- **原始数据保护**: `data1014.xlsx` 从未被修改，所有产出在 `_output/` 子目录

---

## 十、已知局限

1. **KG 数据量**: 当前 Neo4j 中只有 1 个系统（结构子系统）的数据。已生成 `fmea_merged_final.xlsx`（52 个系统）但尚未导入 Neo4j
2. **few-shot 质量**: 6 组 few-shot 由 LLM 生成，未经过人工审核
3. **Humanise 词典**: 200+ 条缩写 + 120+ 条拼音混淆，覆盖范围需通过更多真实数据验证和补充
4. **S/O/D 评分**: NHTSA 提取的 S/O/D 为 LLM 推断，非真实 FMEA 评分
5. **评估未改造**: 原项目 Turing Test / Ranking Test 评估模块不适用于中文

---

## 十一、Git 提交记录

```
6366dbb (HEAD -> main, origin/main) Phase 1-3: 中文 FMEA 适配改造完成
03ae952 迁移至 DeepSeek V4 + 中文 FMEA 数据集扩展
```

---

*报告生成时间: 2026-05-07 21:00 CST*
*项目路径: D:\code\MWO\LLM-KG-Synthetic-MWO*
*远程仓库: https://github.com/shushu0sama/FMEA-MWO.git*
