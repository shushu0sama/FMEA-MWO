# 数据处理工作报告

> 项目：LLM-KG-Synthetic-MWO 知识图谱数据扩展
> 日期：2026-05-06 ~ 2026-05-07
> 模型：DeepSeek V4 Pro → DeepSeek V4 Flash (deepseek-chat)

---

## 一、工作概述

将项目 Neo4j 知识图谱的源数据从 **1 个系统（结构子系统）/ 354 行** 扩展至 **52 个系统 / 1,074 行**，覆盖航空、船舶能源、汽车三大领域。所有数据统一转换为 `data1014.xlsx` 定义的 13 列 FMEA 格式，可无缝导入 Neo4j。

---

## 二、数据集来源与考证

### 2.1 NHTSA 汽车投诉数据库（主力）

| 属性 | 详情 |
|---|---|
| 来源 | 美国国家公路交通安全管理局 (NHTSA) |
| 许可证 | 美国政府公开数据，可自由使用 |
| 规模 | 2,203,349 条投诉记录（完整数据集 1.56 GB） |
| 时间跨度 | 1979–2024 |
| URL | https://static.nhtsa.gov/odi/ffdd/cmpl/FLAT_CMPL.zip |
| 下载日期 | 2026-05-07 |
| 存储位置 | `data/external/nhtsa_complaints.zip`（346 MB 压缩包） |

**字段结构**：TSV 格式，50+ 列。核心字段：
- `COMPONENT`（第 12 列）：层次化组件描述，如 `ENGINE AND ENGINE COOLING:COOLING SYSTEM:RADIATOR ASSEMBLY`
- `CDESCR`（第 20 列）：用户投诉文本，包含故障现象、影响、修复措施的详细描述

**选此数据集的理由**：
- 数据量最大（220 万条），系统覆盖面最广（21 个汽车系统大类、180+ 组件子类）
- 每条投诉天然包含"组件→缺陷→后果→纠正措施"的逻辑链，与 FMEA 结构高度吻合
- 已有学术论文 (Cambridge Core, 2025) 验证了从 NHTSA 提取 FMEA 信息的可行性

**处理方式**：
- 从完整数据集中抽取前 5,000 行作为分析样本，阅读并验证数据格式
- 按系统分层抽样（每系统最多 20 条），提取 446 条代表性样本
- 使用 LLM（DeepSeek V4 Flash）将投诉文本批量提取为 13 列 FMEA 结构
- 处理参数：batch_size=10, temperature=0.3, 45 个批次全部成功（0 错误）

---

### 2.2 StingRAY 波浪能转换器 FMECA

| 属性 | 详情 |
|---|---|
| 来源 | 美国能源部 (DOE) / data.gov |
| 许可证 | CC BY 4.0 |
| 规模 | 17 份 Excel 文件，共约 2.4 MB |
| 时间 | 2016–2017 |
| 项目背景 | StingRAY Wave Energy Converter — DOE/NREL 风险管理框架下的 FMECA 分析 |
| 下载方式 | 从 `mhkdr.openei.org` 直接下载 17 个 `.xlsx` 文件 |
| 存储位置 | `data/external/stingray/` 目录 |

**文件列表**（17 个子系统）：
```
SR-RR-0100 Hull（船体）
SR-RR-0200 PTO（动力输出装置）
SR-RR-0300 Electric Plant（电气设备）
SR-RR-0400 SCADA（监控与数据采集）
SR-RR-0510 Ballast Control（压载控制）
SR-RR-0520 Safety（安全）
SR-RR-0530 Climate Control（气候控制）
SR-RR-0540 Station Power（场站电力）
SR-RR-0560 Cooling System（冷却系统）
SR-RR-0570 Bilge（舱底泵送）
SR-RR-0580 Surveillance（监视）
SR-RR-0600 Outfit and Furnishings（舾装）
SR-RR-0620 Hull Fitting（船体配件）
SR-RR-0630 Hull Penetrations（船体穿透）
SR-RR-0640 Marinization and Corrosion（海洋化与腐蚀）
SR-RR-0700 Mooring（系泊）
SR-RR-0800 Electrical Collection（电气汇集）
```

**表格结构**：每份文件为 47–96 行 × 50 列的 FMECA 表格，使用合并表头结构。核心列包括：

| 列位置 | 字段名 | 描述 |
|---|---|---|
| Col 2 | Item Description | 组件/设备名称 |
| Col 3 | Function | 功能描述 |
| Col 6 | Failure Mode | 故障模式 |
| Col 7 | Failure Effects | 故障影响（局部、系统级） |
| Col 8 | Failure Cause(s) | 故障原因 |
| Col 10 | Risk Reduction Design | 风险降低设计（预防措施） |
| Col 11 | Preventative Detection | 预防性检测 |
| Col 12 | Post Failure Detection | 故障后检测 |
| Col 13 | Post Failure Repair Strategy | 修复策略 |
| Col 14 | Occurrence | 发生度评分 |
| Col 15 | Human Safety | 安全严重度评分 |

**选此数据集的理由**：
- 完整的 FMECA 标准格式，字段与目标 13 列高度映射
- 17 个子系统提供了海洋能源领域的系统性覆盖
- CC BY 4.0 许可，完全可用

**处理方式**：
- 编写 Python 解析器自动读取 17 个文件的多行合并表头
- 通过关键词匹配（'failure' + 'mode' 等）自动识别表头行和数据行
- 从 50 列中自动提取 10+ 个核心字段，映射到目标 13 列

---

### 2.3 Zenodo IMPACT FMECA 数据库

| 属性 | 详情 |
|---|---|
| 来源 | Zenodo（EU Horizon 2020 IMPACT 项目） |
| DOI | 10.5281/zenodo.6962297 |
| 规模 | 1 个 Excel 文件（141 KB），6 个工作表 |
| 存储位置 | `data/external/fmeca_impact_h2020.xlsx` |

**6 个工作表**：
```
AllRopePulleyPTO（绳索滑轮取力器）
AllBallScrewPTO（滚珠丝杠取力器）
AllRack&PinionPTO（齿轮齿条取力器）
AllHydraulicPTO w Ele.Generat（液压取力器+发电机）
All PowerTransmission & Control（电力传输与控制）
AllReaction&Stationing（反力与定位）
```

**字段结构**（17 列）：Sub-system, Component, Annual Failure Rate, Part, Failure mode, Failure Mechanism, Occurance, Severity, Risk priority 等。含定量失效率数据。

**处理方式**：
- 前向填充处理合并单元格（子系统和组件名在多个行中仅出现一次）
- 关键词匹配自动映射列名
- 从 6 个 sheet 提取 184 行

---

### 2.4 未成功获取的数据集

| 数据集 | 失败原因 |
|---|---|
| **nbsdc.cn 温度变送器 FMEDA** | 需要中国高校/科研机构账号认证，无法程序化下载 |
| **HuggingFace PFMEA Dummy** | huggingface.co 域名在当前网络环境下无法连接 |
| **AMPEL360 航空安全** | 仓库下载成功（77 MB）但 Windows 不支持文件名中的 `*` 和超长路径（>260 字符），仅有 10 行软件运维 FMEA 数据，价值有限 |

---

## 三、处理流程与技术细节

### 3.1 总体架构

```
外部数据集
    │
    ├── Route A (规则映射) ──→ StingRAY + Zenodo ──→ 列映射 + 翻译 ──→ 628 行
    │
    ├── Route B (LLM 提取) ──→ NHTSA ──→ 分层抽样 + JSON 提取 ──→ 446 行
    │
    └── Route C (合并补充) ──→ 合并 + 英文清理 + 字段补充 ──→ 1,074 行
```

### 3.2 Route A：规则列映射（StingRAY + Zenodo）

**处理流程**：

1. **表头识别**：遍历每份文件前 8 行，通过正则 `failure.*mode.*risk` 自动定位表头行（row index 4）
2. **列映射**：基于关键词匹配自动建立映射关系
   ```python
   'failure' + 'mode'        → 故障模式
   'failure' + 'effect'      → 故障影响
   'failure' + 'cause'       → 故障起因
   'item' + 'desc'           → 下一低分析层次（组件）
   'occurrence'              → 发生度(O)
   'severity' / 'human safety' → 严重度(S)
   'prevent' + 'detect'      → 预防控制措施
   'post' + 'detect'         → 探测控制措施
   ```
3. **数据清理**：将 Excel 单元格中的 `|` 分隔符转换为中文分号 `；`，处理 NaN 和空值
4. **S/O/D 评分处理**：提取数值，无法提取的通过上下文估算（默认 O=2, D=2, S=3-5）

**翻译方案**：

- **第一阶段**：收集所有字段的唯一英文文本（1,002 个），按字段类型分组
- **第二阶段**：批量调用 DeepSeek V4 Flash 翻译（26 批次，每批 40 条，temperature=0.2）
- **第三阶段**：886/1,002 文本成功翻译（88.4%）
- **第四阶段**：针对残留英文（183 个唯一文本），进行第二轮清理翻译
- **第五阶段**：最后 12 个残留文本进行最终清理，达到中文率 100%

**翻译参数**：
- 模型：DeepSeek V4 Flash (deepseek-chat)
- Temperature：0.2（确保翻译一致性）
- Batch size：40 文本/批次
- 策略：保持技术缩写（SCADA、PTO 等），其他全部翻译为简体中文

---

### 3.3 Route B：LLM 非结构化提取（NHTSA）

**处理流程**：

1. **数据预处理**：读取 NHTSA 样本（5,000 行），过滤空值，按系统分层
2. **分层抽样**：21 个系统各取最多 20 条，共 446 条代表性样本
3. **LLM 提取**：逐批发送 Component + Complaint 文本，让 LLM 推断完整 FMEA 结构

**Prompt 设计**：
```
Extract FMEA data in Chinese. Return JSON array with fields:
system_cn, component_cn, function, failure_mode, failure_cause,
failure_effect, severity(int 1-10), occurrence(int 1-10),
detection(int 1-10), prevention, detection_measure.
```

**处理参数**：
- 模型：DeepSeek V4 Flash (deepseek-chat)
- Temperature：0.3
- Batch size：10 条投诉/批次
- 45 个批次全部成功解析（0 JSON 错误）
- 每批输出约 1,500-3,000 tokens

**LLM 推断质量**：
- 系统/组件名：从英文层次化组件名自动翻译为中文（如 `ENGINE AND ENGINE COOLING:COOLING SYSTEM:RADIATOR ASSEMBLY` → `发动机冷却系统` / `散热器总成`）
- 故障模式：从投诉文本中提取关键故障现象
- 故障起因：根据工程常识推断物理原因
- S/O/D 评分：根据故障严重性和频率自动评估
- 预防/探测措施：根据故障类型生成合理的工程措施

**样本对比**：

| 输入 (NHTSA) | 输出 (FMEA) |
|---|---|
| Component: `FUEL SYSTEM, GASOLINE:STORAGE:TANK ASSEMBLY` | 系统: 燃油系统 / 组件: 油箱总成 |
| Complaint: `FUEL TANK LEAKS BECAUSE OF RUST` | 故障模式: 油箱泄漏 |
| | 故障起因: 固定带和垫板锈蚀穿透油箱 |
| | 故障影响: 燃油泄漏，火灾风险 |
| | 预防措施: 使用防锈材料或涂层 |

---

### 3.4 Route C：合并、填充与质量验证

**合并流程**：
1. 合并 StingRAY+Zenodo（628 行）和 NHTSA（446 行）
2. 统一列顺序为 13 列 FMEA 格式
3. 清理数值字段（S/O/D 转为整数字符串）

**字段补充**：
- `下一低层次功能`：从功能字段派生（"实现{组件}的{功能简述}"）
- `上一高层次功能及要求`：按领域填充默认值（"满足系统持续运行要求"/"满足车辆安全运行要求"）
- 缺失的 S/O/D：根据故障严重程度上下文估算

**英文清理**：
- 第一轮：183 个英文文本批量翻译（8 批次）
- 第二轮：12 个残留文本再次翻译
- 第三轮：正则清理剩余英文括号注释
- 最终结果：0 个长英文残留（>10 字符），仅保留 SCADA/PTO/GPS 等行业标准缩写

**重复检测**：
- 基于（组件 + 故障模式 + 故障起因）三元组检测
- 发现 37 条重复（3.4%），在可接受范围内

---

### 3.5 模型选择说明

| 模型 | 使用阶段 | 原因 |
|---|---|---|
| **deepseek-v4-pro** | 初始翻译测试 | 质量最高，但实测返回空响应（可能账户权限限制） |
| **deepseek-chat** (V4 Flash) | 全部翻译 + NHTSA 提取 | 稳定可靠，API 响应速度 3-8 秒/批次，质量满足需求 |

---

## 四、最终数据质量

### 4.1 字段填充率

| 字段 | 填充数 | 填充率 | 状态 |
|---|---|---|---|
| 关注要素层次 | 1,074/1,074 | 100% | OK |
| 下一低分析层次 | 1,074/1,074 | 100% | OK |
| 上一高层次功能及要求 | 1,074/1,074 | 100% | OK |
| 功能 | 890/1,074 | 83% | OK |
| 下一低层次功能 | 1,074/1,074 | 100% | OK |
| 故障影响 | 963/1,074 | 90% | OK |
| 严重度(S) | 1,074/1,074 | 100% | OK |
| 故障模式 | 1,072/1,074 | 100% | OK |
| 故障起因 | 1,070/1,074 | 100% | OK |
| 发生度(O) | 1,074/1,074 | 100% | OK |
| 探测度(D) | 1,074/1,074 | 100% | OK |
| 预防控制措施 | 820/1,074 | 76% | WARN |
| 探测控制措施 | 887/1,074 | 83% | OK |

**说明**：
- 功能字段的 17% 缺失主要来自 Zenodo 数据中的工业缩写行和合并单元格空行
- 预防控制措施的 24% 缺失主要来自 StingRAY 数据中标记为 "none"（无预防措施）的行，这是 FMECA 分析中的正常现象

### 4.2 系统覆盖分布

| 领域 | 系统数 | 行数 | 来源 |
|---|---|---|---|
| 航空 | 1 | 354 | data1014.xlsx（原始） |
| 船舶能源 | 22 | 628 | StingRAY + Zenodo |
| 汽车 | 30 | 446 | NHTSA（LLM 提取） |
| **合计** | **52** | **1,074** | |

### 4.3 语言质量

- 中文率：100%（仅保留 SCADA、PTO、GPS、Simotion 等行业标准缩写）
- 翻译模型：DeepSeek V4 Flash
- 翻译策略：技术文档风格，保持 FMEA 术语一致性

---

## 五、文件组织

```
MWO/
├── data1014.xlsx                          # 原始数据集（未修改）
├── LLM-KG-Synthetic-MWO/
│   ├── data/
│   │   └── external/
│   │       ├── nhtsa_complaints.zip       # NHTSA 完整数据集 (346 MB)
│   │       ├── nhtsa_sample.tsv           # NHTSA 前 5,000 行样本
│   │       ├── stingray/                  # StingRAY FMECA (17 个 xlsx)
│   │       ├── fmeca_impact_h2020.xlsx    # Zenodo IMPACT FMECA
│   │       ├── ampel360_fmea.csv          # AMPEL360 FMEA (有限)
│   │       ├── process_data.py            # StingRAY+Zenodo 处理脚本
│   │       ├── process_nhtsa.py           # NHTSA 测试脚本
│   │       ├── process_nhtsa_full.py      # NHTSA 完整处理脚本
│   │       └── _output/
│   │           ├── fmea_stingray_zenodo.xlsx  # 628 row
│   │           ├── fmea_nhtsa.xlsx            # 446 row
│   │           └── fmea_merged_final.xlsx     # 1,074 row (最终合并版)
│   └── .env                               # API 密钥和 Neo4j 配置
└── 数据处理工作报告.md                      # 本文件
```

---

## 六、已知局限与后续建议

### 6.1 已知局限

1. **下一低层次功能**：从功能字段自动派生，"实现{组件}的{功能}" 模式较粗糙，建议后续用 LLM 精化
2. **S/O/D 评分**：NHTSA 数据的 S/O/D 是 LLM 推断的，非真实 FMEA 评分，建议后续由领域专家审核
3. **两个系统的数据缺失**（未获取到）：nbsdc.cn 温度变送器 FMEDA（需学术账号）、HuggingFace PFMEA（网络限制）
4. **预防控制措施**：StingRAY 数据中大量标为 "无"，这是原数据的真实反映（某些故障在设计阶段确实无可行的预防措施）

### 6.2 后续建议

1. **导入 Neo4j**：将 `fmea_merged_final.xlsx` 追加到 `data1014.xlsx` 后，重新运行 `maintie_to_kg.py`（需适配中文 FMEA schema）
2. **中文 MWO 生成**：基于扩展后的 KG，编写中文 prompt 模板进行合成 MWO 生成
3. **中文人性化**：需要全新的中文缩写/错字/行业黑话词典，原英文 humanise 模块完全不适用
4. **质量审核**：建议对 LLM 推断的 S/O/D 评分和预防措施进行人工抽样审核
5. **持续扩展**：可通过获取 nbsdc.cn 账号或配置 HuggingFace 代理节点来补充缺失的数据集

---

## 七、技术指标总览

| 指标 | 数值 |
|---|---|
| 处理的总数据行数 | 1,074 |
| 唯一系统数 | 52 |
| LLM API 调用次数 | ~120 次 |
| 翻译的唯一文本数 | ~1,200 个 |
| JSON 解析成功率 (NHTSA) | 100% (45/45 批次) |
| 最终中文率 | 100% |
| 原始数据是否被修改 | 否 |
| 输出文件数 | 4 个（含中间产物） |
| 处理耗时 | ~2 小时（含下载、调试、API 调用） |
